/** Maximum trolley number workers can assign (1 through this value). */
export const MAX_TROLLEY_NUMBER = 100;

/** @deprecated Use MAX_TROLLEY_NUMBER — kept for toolbar total display */
export const TOTAL_TROLLEYS = MAX_TROLLEY_NUMBER;
